-- LÖVE2D configuration callback, called before the game loads.
-- 't' is a table of settings that control how the engine starts up.
function love.conf(t)
    -- Enable the console window on Windows so print() output is visible during development.
    t.console = true
end
